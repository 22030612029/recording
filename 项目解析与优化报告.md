# 砚台 · 考研学习管理应用（Rico）— 项目解析与优化报告

> 解析时间：2026-08-25 ｜ 分析对象：Rico.zip 内的完整项目（网页版 + 小程序版）

---

## 一、项目是什么

**砚台**是一个面向 **2027 考研备考**的个人学习管理应用，解决三个核心痛点：

1. **刷题记录散乱、分数变化难以追溯** → 提供结构化的刷题台账
2. **错题与知识点割裂、无法多维检索** → 错题与知识点按「知识模块」天然关联，可组合筛选
3. **缺少可视化学情反馈与个性化建议** → ECharts 四类图表 + 规则驱动的自动化学情报告

数据全部存于**浏览器本地（localStorage）**，纯静态、无后端、离线可用，可部署到 GitHub Pages。

---

## 二、整体架构

项目包含**两套独立前端**：

| 端 | 技术栈 | 状态 |
|---|---|---|
| **网页版（H5）** | HTML5 + CSS3（Grid/Flex + 设计令牌）+ 原生 JS ES6 模块 + ECharts 5 + localStorage | ✅ 功能完整，本次已优化并浏览器验证 |
| **小程序版（Taro）** | Taro 4.1.9 + React 18 + TypeScript + SCSS | ⚠️ **半成品：数据层完整，页面层缺失（详见 §六）** |

### 网页版文件结构

```
Rico/
├── index.html              语义化骨架：侧栏 + 主区 + 底部 Tab + 模态 + Toast
├── css/style.css           设计系统（设计令牌/组件/响应式，单文件分区注释）
├── js/
│   ├── app.js              入口：导航/视图切换/模态/Toast/仪表盘/设置/目标院校
│   ├── storage.js          数据层：schema/CRUD/导入导出/示例数据/差距分析
│   ├── papers.js           刷题模块：录入/列表/分数步进/区间色/筛选排序
│   ├── knowledge.js        错题+知识点：双子标签/多维筛选/关联
│   ├── charts.js           ECharts：趋势/分布/雷达/错题类型/目标差距
│   └── analysis.js         学情报告：规则驱动，自动生成结构化报告
```

---

## 三、功能模块清单（网页版）

### 1. 仪表盘
- 目标院校卡片：目标总分 / 当前估算 / 差距，各科优先级标签（重点提升/保持/达标）
- 4 张统计卡：试卷总数、加权平均得分率、错题总数、薄弱科目
- 迷你成绩趋势折线 + 最近试卷列表
- 空状态引导：录入第一张试卷 / 加载示例数据

### 2. 刷题记录
- 录入/编辑表单（名称/类型/科目/日期/得分/满分/备注），实时评级预览
- 列表筛选：科目、类型、排序（日期/分数），关键词搜索
- **行内分数步进 −5/−1/+1/+5**，即时写回并联动全部图表
- 删除时级联清理关联错题与知识点

### 3. 错题与知识点
- 双子标签：错题本 / 知识点
- 多维筛选：科目、知识模块、错因（或知识点类型）、关键词
- 错题与知识点通过 `module`（知识模块）天然关联 + `linkedKpId` 显式绑定
- 知识点掌握度滑块（0–100），按掌握度分色

### 4. 学情分析
- 目标差距柱状图（当前折算分 vs 目标分）+ 优先级面板
- 四类图表：成绩趋势折线、分数分布柱状、知识点掌握雷达、错题类型分布
- 自动化学情报告：优势领域 / 薄弱环节 / 进步空间 / 个性化建议，可一键复制

### 5. 数据与设置
- 目标院校多目标管理（切换当前/编辑/删除）
- 总体目标百分比、科目增删、存储占用显示
- 导入/导出 JSON 备份、加载示例、清空（均二次确认）

---

## 四、数据模型（localStorage key: `kaoyan_study_data`）

```js
{
  version: 2,
  subjects: ["政治","英语","数学","408"],      // 可自定义增删
  paperTypes: ["真题","模拟卷","专项练习"],
  errorReasons: ["概念混淆","计算错误","审题失误","记忆偏差","时间不足","其他"],
  kpTypes: ["关键知识点","薄弱环节","复习建议"],
  targetScore: 90,                              // 总体目标百分比
  activeTargetId: "",                           // 当前激活目标院校
  targets: [{ id, name, major, note, year,
              subjectTargets: {科目:目标分},
              subjectTotals:  {科目:满分} }],
  papers:    [{ id, name, type, subject, date, score, totalScore, note, createdAt, updatedAt }],
  errors:    [{ id, paperId, question, wrongOption, correctAnswer, reason, module, linkedKpId?, createdAt }],
  knowledge: [{ id, paperId, type, module, content, mastery(0-100), createdAt }],
}
```

**关键设计**：错题与知识点以 `module`（知识模块）关联；分数区间按**百分比**映射（适配数学 150 / 政治 100 等不同满分）：≥90% 优秀 / 70–89% 良好 / 60–69% 及格 / <60% 待提升。含「专业课 → 408」自动迁移逻辑。

---

## 五、本次优化内容（网页版）

| # | 问题 | 优化 |
|---|---|---|
| 1 | **「当前估算总分」三处口径不一致**：仪表盘用「已刷科目原始平均分之和」（未刷科目不计）、学情报告用「原始平均分直接相加」（数学 150 制 + 政治 100 制直接相加，不可比）、差距面板用「按目标满分折算后相加」——同一数据三处显示三个数 | 在 `storage.js` 新增统一函数 `estimateTotal()`：各科按「目标满分折算」后相加、未刷卷科目按 0 计入，与目标总分同一量纲；仪表盘 / 学情报告 / 差距面板三处全部改为调用该函数 |
| 2 | `app.js` 中 `window.scrollTo({ behavior: "instant" })` 使用非标准取值 | 改为标准值 `"auto"` |
| 3 | `charts.js` renderRadar 循环内反复调用 `store.getData()`（多余重读） | 改为复用局部 `data` 变量 |
| 4 | `papers.js` / `knowledge.js` 搜索时直接对字段 `.toLowerCase()`，字段为 null 时会崩溃 | 统一加 `String()` 保护 |

### 优化前后对比（以示例数据验证）
- 数学（卷均 86.3/150，得分率 64.8%）、英语（卷均 60/100）、政治（40.7/100）、408（88/100）
- **优化后统一结果**：目标总分 **370**，当前估算 **370.5**（数学折算 97.2 + 英语 60 + 政治 81.3 + 408 折算 132），已超出 **0.5** —— 三处显示一致 ✓

### 验证情况
- ✅ 全部 6 个 JS 文件通过 `node --check` 语法校验
- ✅ 核心计算逻辑用模拟数据运行验证，数字精确匹配
- ✅ 真实浏览器端到端走查（本地 HTTP 服务）：
  - 仪表盘/刷题记录/错题与知识点/学情分析 全部正常渲染
  - 加载示例数据、分数步进联动（44→45 即时写回）、确认弹窗、toast 均正常
  - 全程控制台无任何 JS 报错

---

## 五·二、本次优化与补全（小程序版）

小程序版原为**半成品**：数据层完整（Store/helpers/types/Card），但 `app.config.ts` 注册的 9 个页面全部缺失、Card 样式缺失、模板残留多，**无法编译**。本轮完成：

### 1. 补全 9 个页面（每个 .tsx + .module.scss + .config.ts）
| 页面 | 路由 | 说明 |
|---|---|---|
| 仪表盘 | `pages/dashboard` | 目标院校差距卡（统一口径估算）+ 4 统计卡 + 最近试卷 + 空状态引导 + 示例数据 |
| 刷题记录 | `pages/papers` | 科目筛选 chips + 分数步进（−5/−1/+1/+5 即时写回）+ 编辑/删除 |
| 错题与知识点 | `pages/errors` | 双子标签 + 科目/模块/错因（类型）多维筛选 + 关联展示 + 掌握度进度条 |
| 学情分析 | `pages/analysis` | 总览统计 + 目标差距面板 + 学情报告（自动生成 + 一键复制） |
| 设置 | `pages/settings` | 目标院校管理 + 总体目标 slider + 科目管理 + 数据导出/导入/示例/清空 |
| 试卷表单 | `pages/paperForm` | 录入/编辑，实时评级预览，得分超满分提示 |
| 目标院校表单 | `pages/targetForm` | 动态科目目标分/满分 + 目标总分实时汇总 |
| 错题表单 | `pages/errorForm` | 关联试卷/错因/知识模块/关联知识点 |
| 知识点表单 | `pages/knowledgeForm` | 关联试卷/类型/掌握度 slider |

### 2. 修复的原模板 bug
- `helpers.ts` 使用了 `Taro.getStorageSync` 却**未导入 Taro**（会导致编译失败）→ 补导入
- `components/Card/index.module.scss` **缺失**（Card import 了但文件不存在）→ 补全样式

### 3. 去掉没用的东西（清理）
- ❌ `src/pages/index`（模板欢迎页，未注册）→ 删除
- ❌ `src/services/cloud.ts`（引用不存在的 `src/data/*`，纯本地存储无需云）→ 删除
- ❌ `src/styles/compat.scss`（无任何引用）→ 删除
- ❌ `src/assets/tabbar/*.svg`（非有效 tabBar 图标且小程序不支持 svg 格式）→ 删除，tabBar 改为纯文字
- ❌ `package.json` 中未使用的 `zustand` 依赖 → 移除
- ❌ `src/app.tsx` 空生命周期（模板残留）→ 精简为仅挂载 StoreProvider
- ❌ `project.config.json` 的 `cloudfunctionRoot`（无云函数）→ 移除，项目名更新为 `yantai-kaoyan`
- 🔧 `StoreContext.tsx` 的 `addError` 可能写入 `undefined` 的 `linkedKpId` → 修正为 `''`
- 🔧 顺带修复 `StoreContext` 未使用的 `Taro` 导入

### 4. 增强的数据层
- `helpers.ts` 新增 `estimateTotal()`（统一口径，与网页版一致）、`computeSummary()`（总览统计）、`parseImport()`（导入 JSON 校验）、`estimateSize()`（存储占用）、`formatDate()`、`tierColor()`
- `StoreContext` 新增 `setTargetScore()`（总体目标百分比）与 `importData()`（剪贴板 JSON 导入）

### 5. 验证情况
- ✅ 全部 24 个 TS/TSX 文件通过 esbuild 语法校验（0 报错）
- ✅ `estimateTotal` / `computeSummary` / `buildReport` 逻辑用 Node 实测：目标 370 / 估算 150 / 差 220 精确匹配，报告文本完整
- ✅ 9 个页面 + Card 组件样式类名全部定义齐全（无缺失类）
- ⚠️ 本机 `npm install` 在运行时会持续挂起（环境问题，非代码问题），未能执行完整 Taro 构建；请在本地 `npm install && npm run build:tt` 验证真机构建

---

## 五·三、新增功能：错题 / 知识点图片上传

本轮在**错题与知识点**中新增**图片上传**能力，网页版与小程序版两端同步支持。

### 1. 网页版实现
- 图片经 **Canvas 压缩后转为 base64 dataURL** 存入 localStorage（随 `kaoyan_study_data` 持久化，随导入/导出一并迁移）
- `knowledge.js` 新增 `fileToCompressedDataUrl()`：最长边压缩至 **900px**、JPEG 质量 **0.72**，文件大小限制 **8MB**（超限 toast 提示），读取失败安全回落
- `pickImage()` 复用隐藏 `<input type="file">` 完成选图；`openImageViewer()` 提供点击大图预览模态

### 2. 小程序版实现
- 选图流程：`chooseImage` → `compressImage(quality 70)` → `saveFile` 持久化为**本地路径**并写入 Taro storage；持久化失败时回退为**临时路径**
- `utils/image.ts` 新增 `chooseAndSaveImage()` 封装上述流程

### 3. 数据模型
- `ErrorItem` / `KnowledgeItem` 均新增**可选字段** `image`（网页版为 dataURL，小程序版为本地/临时路径）；旧数据无该字段时按空处理，向后兼容

### 4. 列表缩略图与预览
- 两端列表均渲染**缩略图**，**点击可放大预览**（网页版 `openImageViewer` 模态，小程序版 `pages/errors` 内预览）

### 5. 两端 JSON 导出互通说明
- **网页版**：`image` 为 base64 dataURL，导出 JSON 后**两端均可直接渲染**
- **小程序版**：`image` 为本地文件路径，**仅同机可用**；导出 JSON 后仅**网页端可预览**，小程序端无法跨机引用本地路径

### 6. 涉及文件清单
| 端 | 文件 |
|---|---|
| 网页版 | `js/storage.js`、`js/knowledge.js`、`index.html`、`css/style.css` |
| 小程序版 | `miniapp/src/types/index.ts`、`miniapp/src/utils/image.ts`、`miniapp/src/store/StoreContext.tsx`、`miniapp/src/pages/errorForm/`、`miniapp/src/pages/knowledgeForm/`、`miniapp/src/pages/errors/` |

---

## 六、发现的问题与建议

### ✅ 已解决：小程序版页面层缺失

原 `miniapp/src/pages/` 下只有模板默认页 `index`，`app.config.ts` 注册的 **9 个页面**（`dashboard / papers / errors / analysis / settings / paperForm / targetForm / errorForm / knowledgeForm`）全部缺失，且 `Card` 样式缺失、`helpers` 未导入 `Taro`、模板残留多，**无法编译**。

本轮已**全部补全**并清理（详见 §五·二）：
- ✅ 9 个页面 × 3 文件（.tsx + .module.scss + .config.ts）全部就位
- ✅ 修复模板遗留 bug（helpers 缺 Taro 导入、Card 缺样式）
- ✅ 删除无用残留（cloud.ts / compat.scss / 模板页 / 无效 svg 图标 / zustand 依赖 / 空生命周期）
- ✅ 24 个 TS/TSX 通过 esbuild 语法校验，核心逻辑 Node 实测通过

### 🟡 中优先级
1. **科目差距展示口径**：仪表盘科目卡显示「原始平均分 vs 目标分」（数学 86.3 → 120），而总分估算用「折算分」——用户看到"数学差 33.7 分"却"总分已超出"会有轻微困惑。建议在科目差距文案中补充一句折算说明
2. **ECharts 依赖 CDN**：`cdn.jsdelivr.net` 加载失败时图表降级为提示文字；若需完全离线，可将 echarts.min.js 本地化

### 🟢 低优先级
- 小程序端 tabBar 当前为纯文字（原 svg 占位图非有效图标且小程序不支持 svg）；如需图标，请将 png 图标放入 `src/assets/tabbar/` 并在 `app.config.ts` 的 tabBar list 中配置 `iconPath`/`selectedIconPath`
- 可考虑给网页版加 PWA 离线缓存，进一步强化"离线可用"卖点

---

## 七、部署方式

1. **本地预览**：在 `Rico/` 目录执行 `python -m http.server 8731`，浏览器访问 `http://127.0.0.1:8731/index.html`
2. **GitHub Pages**：推送仓库后 Settings → Pages 选 `main` 根目录即可
3. **数据迁移**：设置页「导出 JSON」→ 新设备「导入 JSON」
